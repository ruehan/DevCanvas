# 리뷰 기록 — 스튜디오 재디자인(대화형 셸)

- 날짜: 2026-07-09
- 브랜치: feat/studio-redesign
- 최종 판정: 통과 (1라운드)

## 배경
멈춰 있던 WIP 이어받음. design.html `view-studio`(ADR-0020) 기반 대화형 셸을
`/studio`에 구현. 좌=대화 패널(empty/loading/ready 상태기계), 우=캔버스(ResultViewer 탭 재사용),
백엔드 `/sessions` API 실연동. 착수 시점 verify 실패(tsc strict) + 기록 누락 상태였음.

## 0라운드 (착수 진단)
- verify-all.sh 실패: web tsc `noUncheckedIndexedAccess` 3건 —
  reducer.test.ts(17,18), conversation-panel.test.tsx(21). 배열 인덱스 접근 후 프로퍼티.
- 반영: 단정 직전 존재 보장되는 지점에 non-null 단언(`[0]!`) 적용. verify EXIT 0 복구.

## 1라운드 (code-reviewer)
- 판정: 수정 필요
- 검증: verify-all.sh EXIT 0, pnpm build 성공(/studio 7.9kB).
- 지적사항:
  - [P1-1] 연타 시 경쟁 조건 — `state.phase==="loading"` 가드 없어 중복 세션 생성 가능(page.tsx handleSubmit).
  - [P2-2] FSD 공개 API 위반 — page.tsx 가 `@/features/studio/ui/conversation-panel` 내부 경로 직접 import.
  - [P2-3] messages.map 키로 배열 인덱스 사용.
  - [P2-4] 전송 시 사용자 메시지 즉시 추가 → API 실패해도 스레드에 잔존.
- 반영:
  - P1-1: handleSubmit 진입 시 `if (state.phase === "loading") return;` 가드 추가.
  - P2-2: features/studio/index.ts + ui/index.ts 공개 API 신설(ConversationPanel/studioReducer/initialState/타입). page.tsx 를 `@/features/studio` 단일 배럴 import 로 정리.

### 이월(non-blocking)
- P2-3: 인덱스 키 — 현 reducer 는 메시지를 append 또는 전체 reset 만 하고 재정렬/삭제가 없어 안전. Message 에 id 도입은 스트리밍/부분갱신 사이클에서 검토.
- P2-4: 실패한 사용자 메시지 잔존 — 채팅 UX 상 "무엇을 요청했는지" 유지가 정상. 자동 재시도 없음(사용자 재입력). 그대로 둠.

## 커밋 내역
- (feat): 스튜디오 대화형 셸 + /sessions 연동 (0라운드·1라운드 반영 포함)
