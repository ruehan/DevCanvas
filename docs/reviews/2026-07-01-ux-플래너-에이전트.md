# 리뷰 기록 — UX/스크린/상태매트릭스 에이전트 상세 구현
- 날짜: 2026-07-01
- 브랜치: feat/ux-planner-agent
- 최종 판정: 통과 (2라운드)

## 1라운드
- 판정: 수정 필요
- 검증: verify-all.sh EXIT 0. api ruff/mypy strict(34파일)/pytest 54개. web 변경 없음.
- 지적사항:
  - [P2-1] _build_flows names[:3] 잘림 + 죽은 screen_type 인자 → 다중 엔티티 두번째 흐름 누락, flows 항상 길이 1.
  - [P2-2] flows 동작 검증 테스트 없음.
  - [P2-3] ADMIN+빈 requirement 폴백 / INTERNAL_TOOL / _entity_label 미지정 엔티티 세 분기 테스트 0건.
  - [P2-4 선택] data_columns generic(엔티티 미반영).
  - [P2-5 선택] 에이전트 시그니처 순서 불일치(ux_planner vs design_system).
  - [P2-6] worklog/reviews 누락(최종 판정 후 합의).
- 반영: 커밋 3762c55.

## 2라운드
- 판정: 통과
- 검증: verify-all.sh EXIT 0. api ruff/mypy strict(34파일)/pytest 59개. web 통과.
- 신규 결함: 없음

### 1라운드 반영 상세 (커밋 3762c55)
- P2-1: _build_flows 재설계 — 각 엔티티마다 [대시보드?]→{entity}목록→{entity}상세 흐름 1개씩 생성(엔티티별 별도 흐름). 죽은 screen_type 인자 제거 → has_dashboard bool + labels + screens 인자로 변경.
- P2-2: test_flows_cover_every_entity(엔티티2→흐름2, 두 엔티티 모두 등장), test_dashboard_flow_prefixes_dashboard 추가.
- P2-3: test_admin_empty_requirement_falls_back_to_single_list, test_internal_tool_treated_as_list_no_dashboard, test_unknown_entity_label_passes_through 추가.
- ADR-0010 영향 절: 주화면 정책(DASHBOARD만 대시보드, ADMIN/INTERNAL_TOOL list), flows 정책(엔티티별 흐름), _entity_label 패스스루 명시.

### 이월 메모 (다음 진입 전 검토 권장, non-blocking)
- P2-5: 에이전트 시그니처 순서 일관화(항상 generation_input 우선). ADR-0007 시그니처 정리 시점이 적기.
- P2-4: data_columns 도 엔티티 매개변수화(actions/state 와 일관).
- N1: _build_flows 의 screens 매개변수 용도 협소(폴백만 사용) — labels 기반 폴백으로 단순화 권장.
- N2/N3: flows 테스트 체인 완결성 단정 강화 권장.

## 커밋 내역
- cb8eaf1 docs(adr): UX 플래너 에이전트 규칙 기반 결정(0010)
- 8bf90ce feat(ux): 화면·State Matrix 규칙 기반 도출 및 종류별 상태 템플릿 추가
- 3762c55 refactor(ux): 리뷰 1라운드 반영 — flows 재설계·죽은 인자 제거·폴백/INTERNAL_TOOL/라벨 테스트 추가
