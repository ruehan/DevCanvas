# 리뷰 기록 — Sandpack 통합(milestone #7)
- 날짜: 2026-07-08
- 브랜치: feat/sandpack-preview
- 최종 판정: 통과 (2라운드)

## 1라운드
- 판정: 수정 필요
- 검증: verify-all.sh EXIT 0. web tsc/lint/vitest 24개.
- 지적사항:
  - [P1] component_tree esc 미적용 → JSX 표현식/요소 인젝션.
  - [P1] title esc 맥락 틀림(JSX text 인데 \," 만).
  - [P2] state 개행 미처리 → JS SyntaxError.
  - [P2] esc 테스트 0건.
  - [P2] import @/features/preview/ui → public API 로.
  - [P2] SandpackFiles 타입명 라이브러리 충돌.
- 반영: 커밋 722f6bf.

## 2라운드
- 판정: 통과
- 검증: verify-all.sh EXIT 0. web tsc/lint/vitest 27개.
- 신규 결함: 없음
- 보안 회귀 주입(리뷰어): component_tree `{alert}`/`<script>` 쌍, title `</h1><script>`, state `"};alert();//`, 백틱, U+2028/U+2029, null 바이트 — 전부 JS string literal 캡슐화(createElement("script") 미등장). 인젝션 차단 확인.

### 1라운드 반영 상세 (커밋 722f6bf)
- P1/P2 escape: 동적 문자열 전부 JSON.stringify → JSX string expression `{"<리터럴>"}`. 잘못된 esc 제거. STATES 도 JSON 객체.
- P2: PreviewFiles rename, @/features/preview public API import.
- 테스트 9개: 인젝션(따옴표 리터럴 단정 + bare block 부정), 개행 유효 코드, 빈 tree "(컴포넌트 없음)".
- 2라운드 권고(P3) 반영: bare block negative 단정 추가.

## 커밋 내역
- b02c0a2 docs(adr): Sandpack 자급자족 프리뷰 결정(0015)
- 4fc5c47 feat(preview): Sandpack 기반 실시간 프리뷰 및 State Matrix 토글 추가
- 722f6bf fix(preview): 1라운드 반영 — 인젝션 차단·타입 rename·public API import·테스트
