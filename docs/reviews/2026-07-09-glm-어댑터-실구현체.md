# 리뷰 기록 — GLM 어댑터 실구현체(glm-5.2)
- 날짜: 2026-07-09
- 브랜치: feat/glm-adapter-impl
- 최종 판정: 통과 (1라운드)

## 1라운드
- 판정: 통과 (P2/P3 보완 권고)
- 검증: verify-all.sh EXIT 0. api ruff/mypy strict(51파일)/pytest. web 변경 없음.
- 지적사항(권고, non-blocking):
  - [P2] worklog/reviews 기록 누락(병합 전 추가).
  - [P2] _extract_json "여분 텍스트 처리" 테스트 부재(펜스만).
  - [P3] dead `except GenerationError: raise` (try 내에 GenerationError 경로 없음).
- 반영: 보완 커밋(dead except 제거, 여분 텍스트 회귀 테스트 추가).

## 반영 상세
- ADR-0007 보류→채택: 응답검증 정책(어댑터가 model_validate, 실패 시 GenerationError, MVP 재시도 없음). glm-5.2 모델.
- pipeline/llm.py: GenerationError, _build_prompt(model_json_schema + instruction + context), _extract_json(```json``` 펜스 + 평문 섞임 처리), GLMAdapter(http_post 주입, Bearer, response_format json_object, temperature 0.2, timeout 60). model_validate 실패 → GenerationError.
- generations/dependencies: get_llm_adapter 키만 있으면 GLMAdapter(base 기본값).
- settings: glm_model(기본 glm-5.2), glm_api_base(기본 bigmodel v4). httpx 런타임 의존화.
- tests/pipeline/test_glm_adapter.py 9개(정상·요청형태·프롬프트·invalid JSON·스키마위반·http에러·펜스·여분텍스트·키없음). http_post 주입으로 네트워크 격리.
- tests/pipeline/test_llm.py: stub 테스트(NotImplementedError) → 새 정책(GenerationError).

## 커밋 내역
- 7371dc2 docs(adr): LLM 응답검증 정책 확정·glm-5.2 반영(0007)
- e669c88 feat(llm): GLM 5.2 실구현체 추가(httpx 호출·Pydantic 검증·http 주입 테스트)
- (보완) refactor(llm): dead except 제거·여분 텍스트 회귀 테스트
