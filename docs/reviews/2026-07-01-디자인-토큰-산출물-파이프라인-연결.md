# 리뷰 기록 — 디자인 토큰 산출물 파이프라인 연결
- 날짜: 2026-07-01
- 브랜치: feat/design-export-wiring
- 최종 판정: 통과 (2라운드)

## 1라운드
- 판정: 수정 필요
- 검증: verify-all.sh EXIT 0. api ruff/mypy strict(30파일)/pytest 38개. web 변경 없음.
- 지적사항:
  - [P2] 경로 충돌 가드 부재 — code.files + token_files 병합 시 중복 path 방어 없음. 향후 실 LLM code_generator 침범 시 중복.
  - [P2] handoff 가 병합 전 code 받음 → install_commands/guide_md 에 tokens.css import·tailwind 적용 단계 누락. 추적 약함.
  - [P3] 루트 제네릭 파일명(design.md/design.json) 충돌 표면.
- 반영: 커밋 d7946fd.

## 2라운드
- 판정: 통과
- 검증: verify-all.sh EXIT 0. api ruff/mypy strict(30파일)/pytest 41개. web 통과.
- 신규 결함: 없음

### 1라운드 반영 상세 (커밋 d7946fd)
- 충돌 가드: orchestrator._merge_code(app, token) — path 충돌 시 토큰 파일 우선(대체) + logging.warning. exporter.RESERVED_TOKEN_PATHS frozenset 노출(문서/오라클용).
- 테스트 test_pipeline_dedups_code_paths_token_files_win: code_generator 가 tailwind.config.json 침범 → 단일, 토큰 내용 승.
- 경로 네임스페이스: design.md → docs/design.md, design.json → tokens/design.json. tailwind.config.json 은 루트 관례 유지.
- ADR-0009 영향 절: handoff 갭(file_tree·install_commands·guide_md 토큰 미반영)을 명시적 후속 이관으로 기록.

### P3 권고 (non-blocking, 다음 작업 참고)
- RESERVED_TOKEN_PATHS 가 현재 미소비(문서용) — 향후 code_generator 프롬프트/가드에서 실소매.
- _merge_code 는 app↔token 충돌만 처리, app_files 내부 중복 미처리.
- review_agent 도 병합 전 code 받음 → 토큰 파일 미리뷰(결정적 산출이므로 리뷰 불필요, ADR에 한 줄 추가 가능).

## 커밋 내역
- 3ca696e docs(adr): 디자인 토큰 산출물 코드 연결 위치 결정(0009)
- 9a74eff feat(design): 토큰 산출물을 to_code_files 로 생성해 파이프라인 코드에 병합
- d7946fd refactor(design): 리뷰 1라운드 반영 — 경로 충돌 가드·경로 네임스페이스·handoff 갭 이관 명시
