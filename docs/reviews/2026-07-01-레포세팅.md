# 리뷰 기록 — 레포 세팅
- 날짜: 2026-07-01
- 브랜치: feat/repo-setup
- 최종 판정: 통과 (2라운드)

## 1라운드
- 판정: 수정 필요
- 검증: verify.sh api/·web/ 모두 EXIT 0 (ruff/mypy strict/pytest 3개, tsc/next lint/vitest 1개). HARNESS GAP 없음.
- 지적사항:
  - [P1] docs/worklog.md, docs/reviews/ 없음 — 워크플로 8단계(기록) 미충족
  - [P2] api/src/devcanvas_api/main.py:8 — 도메인 public API(`__init__.py`) 우회 import
  - [P2] web FSD 강제 부재 — boundary 센서 없음, 2단계 전 권장
  - [P2] api/pyproject.toml — 빈 `[[tool.mypy.overrides]]`(dead config)
  - [P2] api/.env.example — `DEVCANVAS_DEBUG=true`가 코드 기본값(False)과 불일치
  - [P2] ADR-0005 어댑터 위치 미정 — 2단계로 이월(합의)
- 반영: P1(기록)은 워크플로 순서상 최종 판정 후 작성으로 합의. P2는 커밋 `89bb6e5`에서 일괄 반영(하단).

## 2라운드
- 판정: 통과
- 검증: `bash scripts/verify-all.sh` EXIT 0 직접 확인.
  - api/: ruff pass · mypy strict pass(10파일) · pytest 3개 pass
  - web/: tsc --noEmit pass · next lint pass(boundary 규칙 포함) · vitest 1개 pass
- 신규 결함: 없음

### P2 반영 상세 (커밋 89bb6e5)
- main.py → `from devcanvas_api.core import settings`, `from devcanvas_api.health import router` (public API 경유)
- pyproject.toml — 빈 mypy overrides 블록 제거 (mypy strict 회귀 없음)
- .env.example → `DEVCANVAS_DEBUG=false` + "로컬에서만 true" 주석
- web/.eslintrc.json — `eslint-plugin-boundaries` v6 `boundaries/dependencies` 규칙 추가.
  - 방식: `default: allow` + 각 레이어 upward import `disallow` (FSD 규칙 #1).
  - 외부 패키지(next/react)는 elements 패턴 미매칭 → default-allow 통과, 오탐 없음.
  - `eslint-import-resolver-alias`로 `@/` alias 해결.
- web/src/FSD.md — 강제 범위 명시 (규칙 #1 센서 동작, #2 동일-레이어 슬라이스 간·#3 public API는 향후 슬라이스 단위 element로 확장 예정)

### FSD boundary 센서 검증 방식 (리뷰어 P2 권장 기록)
임시 위반 파일로 규칙 동작을 확인:
- `web/src/features/x/bad.ts` 에 `import { APP_NAME } from "@/app/page"` 작성 → features→app upward 위반.
- `npx eslint src/features/x/bad.ts` → `Dependencies to elements of type "app" are not allowed in elements of type "features". Denied by rule at index 2` 포착 확인.
- 2라운드 리뷰어가 `shared/lib/__review_probe.ts`(shared→app)로 독립 재현 → 동일하게 포착.
- 검증 후 임시 파일 제거, lint 정상 복귀.

## 커밋 내역
- 0a62e58 docs: 프로젝트 기획·계획·ADR 문서 추가
- 650565f feat(api): FastAPI 백엔드 뼈대 및 검증 도구 세팅
- ee6c1f0 feat(web): Next.js 프론트엔드 뼈대 및 FSD 구조 세팅
- eb18f1a chore: 모노레포 전체 검증 래퍼 스크립트 추가
- 89bb6e5 refactor: 리뷰 P2 반영 — public API 일관 사용·mypy dead config 제거·.env 기본값·FSD boundary 강제
