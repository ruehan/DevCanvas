# 리뷰 기록 — 디자인 시스템 에이전트 상세 구현
- 날짜: 2026-07-01
- 브랜치: feat/design-system-agent
- 최종 판정: 통과 (3라운드)

## 1라운드
- 판정: 수정 필요
- 검증: verify-all.sh EXIT 0. api ruff/mypy strict(29파일)/pytest 24개. web 변경 없음.
- 지적사항:
  - [P1] worklog 이번 작업 항목 누락 → 워크플로 8단계 시점 합의(최종 판정 후 작성)
  - [P2] 폴백 silent + 테스트 truthy 만 검사 → B2B primary 검증·경고 로깅
  - [P2] 톤 케이스 불일치("B2B" vs 소문자) → Tone enum 권장
  - [P2] Tailwind fontSize 의도 미명시 + 테스트 누락
  - [P2] 빈 DesignSystem 엣지 테스트 부재
  - [P2] TypedDict 키 존재 검증 부재
  - [P3] _CSS_PREFIX default 분기, 반환타입 가독성, fixture spacing xl 누락
- 반영: 커밋 255e94c.

## 2라운드
- 판정: 수정 필요 (P1 1개 신규)
- 검증: verify-all.sh EXIT 0. api mypy strict 29파일/pytest 31개.
- 지적사항:
  - [P1] GenerationRequest.tone before-validator 누락 — GenerationInput만 정규화 → API 진입점이 {"tone":"B2B"} 거부(주장-코드 불일치). tone 정규화 테스트가 build_design_system만 찔러 사각지대.
  - [P2] build_design_system 폴백 분기가 파이프라인 정상 경로에선 dead code(Tone enum 항상 전달) → ADR 의미 과대 포장.
  - [P3] fixtures "뼌대" 오타.
- 반영: 커밋 37b2761.

## 3라운드
- 판정: 통과
- 검증: verify-all.sh EXIT 0. api ruff/mypy strict(30파일)/pytest 36개. web 통과.
- 신규 결함: 없음

### 반영 상세
- 1라운드(255e94c): Tone StrEnum(소문자) 도입 + GenerationInput.tone before-validator. build_design_system 케이스 무관 정규화 + 폴백 시 logging.warning. presets 소문자 통일. exporter TypeAlias·_CSS_PREFIX 직접 인덱싱·fontSize 의도 docstring. fixtures spacing xl. 테스트 보강(폴백 primary 검증, 케이스 무관, TypedDict 키, 빈 DS 엣지, fontSize assertion). ADR-0008 영향 절 정책 명시.
- 2라운드(37b2761): `_normalize_tone` 공통 함수를 pipeline/schemas.py 에 추출 → GenerationInput·GenerationRequest 양쪽이 동일 정책 적용(API 진입점 {"tone":"B2B"} 수용). tests/test_generations_schemas.py 추가(파라미터화 case-insensitive + "우주인" ValidationError). ADR-0008 폴백 의미 보완(파이프라인 정상 경로 미도달, presets 독립 사용 시 방어). fixtures 오타 수정.

### P3 권고 (다음 작업 반영 권장, non-blocking)
- 크로스 스키마 테스트: req.to_generation_input().tone == GenerationInput(...).tone 단언 → 양 validator 회귀 고정.
- 파라미터화 커버리지: 5개 Tone 전체(B2B/Minimal/Enterprise/Startup/Friendly) case-insensitive 검증.

## 커밋 내역
- d109b2f docs(adr): 디자인 시스템 에이전트 규칙 기반 결정(0008)
- d96a839 feat(design): 톤별 토큰 프리셋 및 다중 포맷 exporter 추가
- 255e94c refactor(design): 리뷰 1라운드 반영 — Tone StrEnum·폴백 로깅·엣지 테스트·exporter 정제
- 37b2761 fix(generations): GenerationRequest tone 정규화 누락 수정 및 스키마 계약 테스트 추가
