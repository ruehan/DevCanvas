# 리뷰 기록 — design.html 기반 디자인 시스템 + 랜딩 + 라우트 분리
- 날짜: 2026-07-09
- 브랜치: feat/ui-landing-design
- 최종 판정: 통과 (2라운드)

## 1라운드
- 판정: 수정 필요
- 검증: verify-all.sh EXIT 0, pnpm build 성공(/ 812B, /studio 5.84kB).
- 지적사항:
  - [P1-1] worklog/reviews 기록 누락.
  - [P1-2] FEATURE 1(studio session)·FEATURE 2(state matrix) preview 블록 누락 — "포팅" 표방이므로 채워야.
  - [P2-3] TopBar "공개 프로젝트" 링크 누락, "스튜디오 열기" CTA 시각 강등.
  - [P2-4] P2 severity 매핑이 accent(warn 아님) — 시맨틱.
  - [P2-5] footer 임의 추가(design.html엔 없음).
  - [P2-6] design.html FEATURE 3 preview 자체가 빈 칸(원천 결함).
  - [P2-7] GmarketSans CDN preconnect 누락(FOUC/LCP).
  - [P2-8] 랜딩 테스트가 preview 누락 못 잡음.
- 반영: 커밋(refactor).

## 반영 상세
- P1-2: StudioSessionPreview(채팅 목업 + 작업단계 5/5 + typing), StateMatrixPreview(6 상태 타일 + 18 variations) 포팅. FEATURE 1 우측 col-span-7, FEATURE 2 좌측 col-span-6(order) 삽입.
- P2-3: TopBar "공개 프로젝트" 링크 + "스튜디오 열기" 채운 버튼(bg-text text-bg) 복원.
- P2-7: layout.tsx 에 cdn.jsdelivr.net preconnect 추가.
- P2-8: landing-page.test.tsx 에 "5/5 완료"/"studio session"/"State Matrix"/"18 variations" 단정 추가.

### 이월(non-blocking)
- P2-4: P2 severity → warn 매핑. warn-soft 토큰 미정의 → 토큰 확장 후 매핑(차기).
- P2-5: footer 임의 추가 — design.html에 없으나 보완. ADR-0019에 명시 검토.
- P2-6: design.html FEATURE 3 preview 빈 칸(원천 결함) — 스튜디오 재디자인 사이클에서 별도 디자인.
- P2-FSD: widgets/landing → widgets/top-bar 동일 레이어 import(규칙 #2 회색지대, lint 통과). 차기에 TopBar→shared/ui 이동 또는 ADR 수용 검토.
- P3: StateTile 레이아웃 단일화(default/loading 미세 시각 차), 테스트 개수 기록 정정.

## 커밋 내역
- 8298b16 docs(adr): DevCanvas 앱 디자인 시스템 채택(0019)
- f1bf405 feat(web): warm neutral 디자인 시스템 + 랜딩 + /studio 분리
- (refactor): 1라운드 반영 — FEATURE preview 포팅·TopBar CTA·preconnect·테스트
