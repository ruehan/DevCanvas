# 리뷰 기록 — GLM 어댑터 + 파이프라인 뼈대
- 날짜: 2026-07-01
- 브랜치: feat/glm-adapter-pipeline
- 최종 판정: 통과 (2라운드)

## 1라운드
- 판정: 수정 필요
- 검증: verify-all.sh EXIT 0. api ruff/mypy strict(24파일)/pytest 6개. web 변경 없음.
- 지적사항:
  - [P1] ADR-0005 시그니처(`generate(prompt,schema)->dict`)가 실제 구현(`generate(schema,instruction,context)->T`)과 충돌, 대체 표시 없음
  - [P2] DummyLLMAdapter._FIXTURES mutable 공유 → 호출부 수정 시 전역 오염
  - [P2] @lru_cache on get_llm_adapter → 런타임 토글 시 stale
  - [P2] ADR-0006 본문 agents/ (디렉토리) vs 실제 agents.py (파일)
  - [P2] worklog/reviews 이번 작업 기록 누락
  - [P2] LLMAdapter 포트 응답 검증/재시도 정책 미정 (3단계 장애물)
  - [P2] agents.py docstring 모순
  - [P2] import 일관성 (core vs core.config)
  - [P2] 경계 테스트 부재
- 반영: 커밋 `b3c604c`(아래). 기록은 최종 판정 후 작성으로 합의(1단계와 동일).

## 2라운드
- 판정: 통과 (조건부 — 기록 작성이 유일 조건)
- 검증: verify-all.sh EXIT 0. api ruff/mypy strict(25파일)/pytest 11개. web 통과.
- 신규 결함: 없음

### 1라운드 반영 상세 (커밋 b3c604c)
- P1: ADR-0005 상태 "부분 대체됨(→0006,0007)" + 갱신 노트(위치·시그니처 변경 명시). 신규 ADR-0007(포트 시그니처 채택 + 응답검증/재시도 정책 보류, GLM 스파이크 후 확정).
- P2 fixture 격리: `DummyLLMAdapter.generate`가 `model_copy(deep=True)` 반환 (test_llm.test_dummy_returns_independent_copy 검증).
- P2 lru_cache: dependencies.py docstring에 캐시 의미론 + `cache_clear()` 안내. (런타임 토글 테스트는 3단계에서 추가 — ADR-0007 보류로 이관, 수용)
- P2 ADR-0006: agents 경로 `agents.py` 정정 + 확장시 디렉터리 분리 명시.
- P2 포트 정책: ADR-0007 "보류" 항목 예약 + agents.py docstring 갱신.
- P2 docstring: "instruction 초안, GLM 도입 시 정제"로 정정.
- P2 import 일관: llm.py·dependencies.py → `from devcanvas_api.core import settings`.
- P2 경계 테스트: tests/pipeline/test_llm.py(unknown schema→NotImplementedError, 복사 격리, GLM stub 미설정/설정 모두 NotImplementedError), test_orchestrator 빈 prompt e2e.

### 3단계 진입 전 권고 (정보성, non-blocking)
- lru_cache 무효화 전략: GLM 토글 테스트 추가 시 cache_clear() 강제 또는 팩터리 패턴 전환 검토.
- 응답 검증/재시도 정책: GLM 스파이크 선행 후 ADR로 확정 (ADR-0007 보류).
- `result.review.findings` 검증 누락: 사소, 3단계 실구현체 도입 시 보강.

## 커밋 내역
- 78b51d6 docs(adr): 파이프라인·LLM 어댑터 위치 결정(0006)
- a796293 feat(pipeline): LLM 어댑터 포트와 7단계 에이전트 파이프라인 뼈대 추가
- 9739e19 feat(generations): POST /generations 엔드포인트 추가
- b3c604c refactor: 리뷰 1라운드 반영 — 더미 fixture 격리·import 일관·경계 테스트·ADR 시그니처 정리(0005/0006/0007)
